import 'package:flutter/material.dart';
class VendorsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Vendors')),
      body: Center(child: Text('Vendors management screen')),
    );
  }
}